# Host Android SDK discovery for the manual mobile example targets
# (//flutter_app:mobile.apk). Harmless when ANDROID_HOME is unset: the
# @androidsdk repo is only fetched when those targets are built.
common --repo_env=ANDROID_HOME

# iOS builds keep the host HOME so CocoaPods caches persist across builds.
build --action_env=HOME
build --java_runtime_version=remotejdk_17
build --tool_java_runtime_version=remotejdk_17
common --repo_env=ACCEPTED_ANDROID_SDK_LICENSE_VERSION=36
